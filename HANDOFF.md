# 專案交接文件（HANDOFF）

給「換電腦後的自己」或協作的 AI 助手看的完整脈絡。讀完這份就能無縫接手。
（README.md 講「怎麼跑起來」；這份講「為什麼這樣做、內容細節、還沒做的」。）

---

## 1. 這是什麼

徐浩華 Ken Chui 的求職作品集網站。深色主題、繁中、面向招募方。
單頁式 React 網站（Vite + Tailwind + React Router），部署在 Vercel。

## 2. 個人定位（很重要，別跑掉）

- 定位：**產品企劃 / APM（會自己動手做的產品人）**，不是「全端工程師 / RD」。
- 背景：**心理學（HKU SPACE 副學士）＋ 商管（台北科技大學經管系，2026 應屆）** 雙背景。
- 一句話：能發現真問題、能算商業帳、也能親手用 React 把產品做出來。
- 「會寫 code」定位成**差異化武器**，不是職稱；不要把他包裝成工程師。
- 現職：CMoney 產品企劃實習（APM），實習仍在進行。
- 聯絡：a96020183@gmail.com ／ 0901-404-044 ／ GitHub a96020183。

## 3. 作品清單與「我負責什麼」（分工誠實，別誇大）

資料集中在 `src/data/profile.js` 的 `projects[]`；每個作品有獨立頁面。

1. **AWS · PressPlay AI 智慧學習助教**（🥈 第二名，代表作 featured）
   - 全端開發＋產品策略＋資料分析由本人獨立完成；簡報由另一位組員上台報告；吉祥物視覺請了設計組員協助。
   - 可互動 Demo 以**靜態內嵌**方式呈現（`public/ppa-demo/`），因競賽 AWS 環境賽後已回收。
2. **法巴 Cardif · 保包巴士**（🥈 第二名，代表作 featured）
   - 提案發想、商業模式、財務模型、技術可行性＝本人；心理測驗 UI 非本人。
3. **瑤光深鯨 · 零硬體無障礙系統**（🥉 第三名）
   - **題目為本人原創發想**；系統設計、商業模式、財務、技術可行性、Figma 互動原型＝本人；組員負責 PPT 與上台。
   - 頁面內嵌**真實可點擊的 Figma 原型**（iframe）。
4. **金孕獎 · 母嬰獎項推廣**（🥈 銀質獎）
   - 受眾/通路/社群/影響者分析、網站升級與遊戲化、口碑行銷 5T、預期效益、成本估算＝本人。
   - 手邊只剩簡報（原始檔 `行銷比賽.pdf`，未進 repo；壓縮版在 `public/jinyun/`）。
5. **ATCC BEACON · 兒少主動式心理支持**（第 23 屆，全國 26 強＝初賽決選）
   - 系統機制設計、OMO 數據漏斗（Mixpanel）、商業模式與三年財務、Roblox 場景＝本人；
     Roblox 概念視覺是**用 AI 工具快速產出**。
   - LINE ARG 校園怪談、實體工作坊**非本人設計**。
6. **2026 第四屆解決未來問題能力競賽**（決賽進行中，`ongoing: true`）
   - 依賽規暫不公開內容，賽後再補完整 case study。目前作品卡不可點擊。

## 4. 履歷 `/cv`

- 用程式刻的頁面（`src/pages/CV.jsx`），右上角可「列印 / 存成 PDF」（列印時自動白底黑字）。
- 舊的圖片版 `public/cv.png` 已**不再連結**（它的 AWS 還寫成「決賽進行中」是舊錯誤）。正本以 `/cv` 為準。
- 工作經歷順序（新→舊）：CMoney → 香港喜來登酒店（夜班房務部經理，2023–2024 合約制）→ 香港東華三院（青少年空間共享計劃經理，2022–2023 合約制）→ Compass Group（採購部文員・兼職，3 年）。

## 5. 設計決策（改動前先知道）

- 首頁能力區叫「**四種能力，一個完整的產品人**」，刻意不用「四個作品」（作品數量會變）。
- AWS 與法巴是**代表作**：橘色外框＋「★ 代表作」標記（`featured: true`）。
- Hero / 數字卡只放**真實事實**，不放推估估值（曾拒絕把「240 萬營收推估、5 萬→1K 漏斗」當主打數字）。
- 跨作品「互補敘事」只放在**首頁**統一講，個別作品頁不重複。
- Demo 用靜態內嵌（不另外部署到 Vercel/Netlify）。

## 6. 部署 / 帳號

- GitHub：`a96020183`，公開 repo，名稱 `ken-chui`。
- Vercel：專案名 `ken-chui`（網址 `ken-chui.vercel.app`）。用 GitHub 連動、push 自動部署。
- `vercel.json` 已設 SPA rewrite（否則 `/cv`、`/projects/*` 直接開會 404）。

## 7. 本機開發雷區（Windows）

- `npx.ps1 cannot be loaded`：PowerShell 執行原則擋住 → 用 `cmd /c "npm run build"` 或 `cmd /c "npx vite build"`。
- 長指令在內建 PowerShell 會觸發 PSReadLine 顯示崩潰 → 指令盡量短，或寫成 .bat 用 `cmd /c` 跑、輸出導到檔案再讀。
- 各種 `*.bat / *.py / *_out.txt / 原始 PDF / CV2.png` 都是做圖用的暫存檔，已在 `.gitignore` 排除，不會進 repo。

## 8. 待辦 / 未定

- 「其他香港工作」若還有沒補到的，補進 `profile.js` 的 `experience`（注意順序新→舊）。
- CV 開頭文案目前統一成「心理學＋商管雙背景的產品企劃」，若要調再改 `CV.jsx`。
- 解決未來問題競賽賽後補完整內容、拿掉 `ongoing`。

## 9. 想在新電腦「延續和 AI 的對話」

本對話是透過 ACP 用戶端進行，session 檔不一定在 `~/.kiro/sessions`，因此不保證能直接複製對話。
最可靠的接手方式＝把這份 `HANDOFF.md` 連同整個 repo clone 下來，讓新的對話讀這份文件即可掌握全部脈絡。
（AWS 專案 repo 內另有一套 `chat-session` 對話搬移包可參考，但仰賴 Kiro 版本的儲存規則，未必適用。）
